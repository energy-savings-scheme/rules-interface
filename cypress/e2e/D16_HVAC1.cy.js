describe('First Test', () => {
  it('Should be true and passed', () => {
    expect(true).to.equal(true)
  })
})

describe('Second Test', () => {
  it('Should be true and failed', () => {
    expect(true).to.equal(false)
  })
})