name: Build and Deploy to DEV

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, labeled]
    branches:
      - nonprod
  push:
    branches:
      - nonprod

jobs:
  build-deploy:
    uses: ./.github/workflows/core.yml
    with:
      run-test: true
      run-deploy: true
    secrets: inherit
